(env
  (dev
    (flags (:standard -warn-error -A))))
    
(library
 (name rampart)
 (libraries sexplib menhirLib)
 (inline_tests)
 (preprocess (pps ppx_inline_test ppx_expect ppx_deriving.show ppx_sexp_conv))
)

(ocamllex lexer)

(menhir
 (modules parser)
 (flags --inspection --table --dump)
)
